{
  "name": "f1-clash-companion-ultimate",
  "version": "1.0.0",
  "private": true,
  "description": "APK wrapper for the F1 Clash Companion web app",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "cap:copy": "npm run build:web && npx cap copy",
    "cap:sync": "npm run build:web && npx cap sync android",
    "android:debug": "npm run build:web && npx cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "chart.js": "^4.5.1",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
